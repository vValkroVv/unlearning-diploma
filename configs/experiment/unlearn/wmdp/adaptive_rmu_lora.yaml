# @package _global_

# Paper-faithful WMDP smoke/tuning config. This is not used by the DUET/RWKU
# campaign wrapper, but keeps the natural Adaptive-RMU setup available.

defaults:
  - override /model: zephyr-7b-beta
  - override /trainer: AdaptiveRMU
  - override /data: unlearn
  - override /data/datasets@data.forget: WMDP_forget
  - override /data/datasets@data.retain: WMDP_retain
  - override /eval: lm_eval

data_split: cyber

data:
  anchor: forget
  forget:
    WMDP_forget:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-forget-corpus.jsonl
  retain:
    WMDP_retain:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-retain-corpus.jsonl

eval:
  lm_eval:
    tasks:
      - wmdp_${data_split}
      - mmlu

collator:
  DataCollatorForSupervisedDataset:
    args:
      padding_side: left

trainer:
  args:
    per_device_train_batch_size: 4
    gradient_accumulation_steps: 1
    learning_rate: 5e-5
    eval_strategy: steps
    eval_steps: 0.5
    max_steps: 500
    lr_scheduler_type: constant
    logging_steps: 10
    save_strategy: steps
    save_steps: 500
    save_total_limit: 2
    remove_unused_columns: false
    gradient_checkpointing: false
    seed: 42
  method_args:
    gamma: 1.0
    alpha: 1200.0
    retain_loss_type: EMBED_DIFF
    steering_coeff: 1.0
    adaptive_scale: 5.0
    adaptive_coeff_mode: first_batch
    module_regex: .*layers\.7$
    trainable_params_regex:
      - .*model\.layers\.(5|6|7)\.mlp\.down_proj\.weight

task_name: wmdp_adaptive_rmu_lora
