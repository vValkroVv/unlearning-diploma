defaults:
  - GradDiff

handler: SatImp

args: # HuggingFace TrainingArguments
  learning_rate: 1e-5
  num_train_epochs: 5

method_args:
  beta1: 5.0
  beta2: 0.1
  alpha: 0.1
  gamma: 1.0
  retain_loss_type: NLL
